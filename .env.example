# Exemplo de variáveis de ambiente. NÃO coloque segredos em arquivos versionados.
# Copie este arquivo para `.env.local` e preencha com valores reais localmente.

# Database Configuration
DATABASE_URL="postgresql://USER:PASS@HOST:PORT/DBNAME?sslmode=require"

# JWT Configuration - DEVE ser uma string aleatória forte em produção
JWT_SECRET="your-super-secure-jwt-secret-key-at-least-32-characters-long"

# Email Configuration (se implementado)
EMAIL_FROM="noreply@galaxiaprofissoes.com"
EMAIL_API_KEY="your-email-api-key"

# Environment
NODE_ENV="development"
